<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/background_color</item>
        <item name="colorPrimaryDark">@color/background_color</item>
        <!-- Other customizations -->
    </style>
    <style name="CustomAlertDialog" parent="Theme.MaterialComponents.Dialog.Alert">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/background_color</item>
        <item name="android:textColorPrimary">#505461</item>
        <item name="android:background">#132155</item>
        <!-- More styling can be added if needed -->
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme2" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Other styles -->

    <!-- Bold Text Style -->
    <style name="BoldText">
        <item name="android:textSize">18sp</item> <!-- Adjust text size as needed -->
        <item name="android:textStyle">bold</item>


        <item name="android:textColor">@color/your_text_color</item> <!-- Define your text color -->
    </style>

</resources>
